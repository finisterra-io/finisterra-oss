/*
  Warnings:

  - You are about to drop the `aws_resource` table. If the table is not empty, all the data it contains will be lost.

*/
-- DropForeignKey
ALTER TABLE "aws_resource" DROP CONSTRAINT "aws_resource_organizationId_fkey";

-- DropTable
DROP TABLE "aws_resource";

-- CreateTable
CREATE TABLE "aws_resources" (
    "id" SERIAL NOT NULL,
    "organizationId" INTEGER NOT NULL,
    "provider" TEXT NOT NULL,
    "accountId" TEXT NOT NULL,
    "region" TEXT NOT NULL,
    "role" TEXT NOT NULL,
    "resourceType" TEXT NOT NULL,
    "resourceName" TEXT NOT NULL,
    "resourceId" TEXT NOT NULL,
    "stateFile" TEXT NOT NULL,
    "scannedStateFile" TEXT NOT NULL,
    "status" TEXT NOT NULL,
    "autogenerated" BOOLEAN NOT NULL,
    "description" TEXT,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" TIMESTAMP(3) NOT NULL,

    CONSTRAINT "aws_resources_pkey" PRIMARY KEY ("id")
);

-- CreateIndex
CREATE UNIQUE INDEX "aws_resources_accountId_resourceType_resourceId_key" ON "aws_resources"("accountId", "resourceType", "resourceId");

-- AddForeignKey
ALTER TABLE "aws_resources" ADD CONSTRAINT "aws_resources_organizationId_fkey" FOREIGN KEY ("organizationId") REFERENCES "organization"("id") ON DELETE RESTRICT ON UPDATE CASCADE;
