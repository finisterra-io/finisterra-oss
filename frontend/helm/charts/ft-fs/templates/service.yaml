apiVersion: v1
kind: Service
metadata:
  name: {{ include "ft-fs.fullname" . }}
  labels:
    {{- include "ft-fs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ft-fs.selectorLabels" . | nindent 4 }}
